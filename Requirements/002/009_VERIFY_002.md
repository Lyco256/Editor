# 009 — verify-002 contract

Create `tools/verify-002.ps1` and `tools/verify-002.sh` with equivalent behavior. Mandatory steps:

1. validate 002 baseline SHA;
2. require no frozen-002 diff;
3. run the supplied Python vector checker;
4. require both pre-freeze reviews `CONFLICTS: 0`;
5. require all 78 original 001 case IDs still exist;
6. prove K004/K005/K006 adapter references canonical vector and both K006 variants execute;
7. fail on the old invalid K004/K005 fixture/expected pairs in active adapter code;
8. fail on ignored/skipped/cfg-disabled required cases;
9. run all acceptance cases;
10. run workspace format, Clippy `-D warnings`, tests, docs mirror;
11. final mode additionally requires final oracle `CONFLICTS: 0` and final product `BLOCKERS: 0`.

Any mandatory command failure returns non-zero; no error suppression.
