# Bundled assets

Mandatory built-in themes and syntax query assets live here. Assets must be deterministic and usable
offline; the initial product performs no runtime grammar or language-server downloads.

