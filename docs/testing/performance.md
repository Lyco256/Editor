# Performance measurements

Release measurements are recorded here from the reference Windows environment: executable and asset
size, empty-workspace resident memory and idle CPU, 10 MiB editing latency, and repeated open/close
memory behavior. A budget exception must include a measured limitation and explicit user approval.

