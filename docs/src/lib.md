# Root library

Role: exposes root application orchestration to the binary and integration tests. It contains no
domain implementation. The `app` module is the single wiring boundary. Covered by root unit and
headless integration tests.

