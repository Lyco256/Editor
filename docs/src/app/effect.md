# Application effects

Role: typed descriptions of asynchronous work. Process specifications keep executable and arguments
separate. The `requires_trusted_workspace` invariant makes every external process kind subject to the
root trust policy before dispatch.

