# Feature registry

Role: stable command metadata used by palettes and dispatch. Registration is deterministic by command
identifier, replacing an existing entry explicitly. The registry owns no service implementation and
performs no I/O.

