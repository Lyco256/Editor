# Configuration core boundary

Role: establishes settings shared by workspace and compatibility layers. The default large-file
threshold is 32 MiB. The Wave 1 implementation adds merging, encoding, EditorConfig, session, and
recovery behavior without process execution.

