# Frame construction

Role: shared helpers for creating semantic framebuffers and status cells. Terminal capability mapping
and escape output remain in `terminal-backend`. View snapshot tests exercise this boundary without a
real terminal.

