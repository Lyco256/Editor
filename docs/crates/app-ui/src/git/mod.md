# Git views

Role: identifies changes, diff, branch, stash, and history presentations. Wave 2 emits typed Git actions,
shows root policy state, and requires confirmations for destructive requests. It never builds commands.

