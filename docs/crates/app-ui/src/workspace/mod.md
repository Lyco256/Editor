# Workspace views

Role: identifies Explorer, Quick Open, search, and trust presentations. Wave 2 consumes streamed model
events and emits typed actions; traversal and writes remain in `workspace-core`.

