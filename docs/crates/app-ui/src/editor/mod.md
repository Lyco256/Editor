# Editor viewport views

Role: stores logical viewport scroll origin separately from text and screen coordinate types. Wave 2
implements text, gutter, selections, markers, folds, and mouse presentation. No document I/O occurs in
draw code.

