# VS Code compatibility boundary

Role: classifies safe static contribution data. Wave 1 adds JSONC, theme, snippet, language config, and
path-safe VSIX handling. Extension executable code is never loaded or run.

