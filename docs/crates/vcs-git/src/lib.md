# Git adapter boundary

Role: distinguishes working-tree and index diff targets. Wave 1 adds structured Git process invocation,
parsers, cancellation, and destructive-operation plans. It never makes Workspace Trust decisions or
depends on terminal UI.

